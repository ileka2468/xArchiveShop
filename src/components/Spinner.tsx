const Spinner = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <div className="w-10 h-10 border-4 border-t-4 border-t-transparent border-gradient-to-r from-purple-500 to-pink-600 rounded-full animate-spin"></div>
    </div>
  );
};

export default Spinner;
